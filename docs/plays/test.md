# Play: Test — evidence inside the loop (v1 self-check)

## v1

1. Session self-check before human review: `bash tests/validate-templates.sh` and `bash tests/validate-hooks.sh`.
2. Optional: markdownlint on `docs/**` and templates.
3. Bugfixes: failing fixture/test first; do not weaken the check to go green.
4. Record Important review findings → feedback loop (`docs/feedback-loop.md`).

## v2 (deferred)

Continuous eval suite on `CLAUDE.md` / skills / hooks changes; 20–50 real tasks.
